// Lovable removed — this file is now a no-op stub kept for import compatibility.
// Error reporting is handled by console.error in error boundaries.
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // no-op
}
